export interface AppResource {
  resourceId: number;            // ResourceId maps to resourceId
  resourceType: string;          // ResourceType maps to resourceType
  resourceValue: string;         // ResourceValue maps to resourceValue
  resourceKey: string;           // ResourceKey maps to resourceKey
}
